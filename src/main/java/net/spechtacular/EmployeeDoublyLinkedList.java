package net.spechtacular;

public class EmployeeDoublyLinkedList {

    private Employee2LinkNode head;
    private Employee2LinkNode tail;
    private int size;

    public void addToFront(Employee employee) {
        Employee2LinkNode node = new Employee2LinkNode(employee);

        if (head == null) {
            tail = node;
        }
        else {
            head.setPrevious(node);
            node.setNext(head);
        }

        head = node;
        size++;
    }

    public void addToEnd(Employee employee) {
        Employee2LinkNode node = new Employee2LinkNode(employee);
        if (tail == null) {
            head = node;
        }
        else {
            tail.setNext(node);
            node.setPrevious(tail);
        }

        tail = node;
        size++;
    }

    public Employee2LinkNode removeFromFront() {
        if (isEmpty()) {
            return null;
        }

        Employee2LinkNode removedNode = head;

        if (head.getNext() == null) {
            tail = null;
        }
        else {
            head.getNext().setPrevious(null);
        }

        head = head.getNext();
        size--;
        removedNode.setNext(null);
        return removedNode;
    }

    public Employee2LinkNode removeFromEnd() {
        if (isEmpty()) {
            return null;
        }

        Employee2LinkNode removedNode = tail;

        if (tail.getPrevious() == null) {
            head = null;
        }
        else {
            tail.getPrevious().setNext(null);
        }

        tail = tail.getPrevious();
        size--;
        removedNode.setPrevious(null);
        return removedNode;
    }

    public int getSize() {
        return size;
    }

    public boolean isEmpty() {
        return head == null;
    }

    public void printList() {
        Employee2LinkNode current = head;
        System.out.print("HEAD -> ");
        while (current != null) {
            System.out.print(current);
            System.out.print(" <=> ");
            current = current.getNext();
        }
        System.out.println("null");
    }


}
